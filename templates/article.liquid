<section class="container">
  <div class="grid-row">
    <div class="grid-item item-s-12 item-m-6">
      <h2>{{ article.title }}</h2>
      <p>{{ article.published_at | date: '%d %B %Y' }}</p>
      {{ article.content }}
    </div>
  </div>
</div>