<section id="page">
  <div class="container">
    <div class="grid-row">
      <div class="grid-item item-s-12">
        <h2>{{ page.title }}</h2>
        {{ page.content }}
      </div>
    </div>
  </div>
</section>
